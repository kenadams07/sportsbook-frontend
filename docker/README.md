# Docker

This folder will hold workspace-level Docker Compose files.

Planned files:

- `docker-compose.dev.yml`
- `docker-compose.prod.yml`

For now, the existing odds-server compose file remains inside `apps/odds-server` until the shared database package is wired.
